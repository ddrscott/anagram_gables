require 'spec_helper'

describe AnagramGables do
  it 'has a version number' do
    expect(AnagramGables::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
